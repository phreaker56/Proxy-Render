{
  "name": "ssh-websocket-proxy",
  "version": "1.0.0",
  "description": "Proxy WebSocket para túnel SSH",
  "main": "index.js",
  "scripts": {
    "start": "node index.js"
  },
  "dependencies": {
    "ws": "^8.0.0"
  }
}
