{
  "name": "ssh-websocket-proxy",
  "version": "1.0.0",
  "description": "Proxy WebSocket para SSH",
  "main": "server.js",
  "scripts": {
    "start": "node server.js"
  },
  "dependencies": {
    "ws": "^8.13.0"
  },
  "engines": {
    "node": ">=18"
  }
}
