{
  "name": "ssh-websocket-proxy",
  "version": "1.0.0",
  "description": "Proxy WebSocket a SSH",
  "main": "server.js",
  "type": "module",
  "scripts": {
    "start": "node server.js"
  },
  "dependencies": {
    "ws": "^8.15.0"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
